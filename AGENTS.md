# 数学建模 Agent 工作约定

## 核心原则

- `grad-ml-template/` 按建模流程组织，其中的算法是可运行基线和接口示例，不是模型白名单。
- 先从赛题目标、数据生成机制、样本规模、时序/空间/分组结构、约束和评价指标推导模型，再决定复用、扩展或替换模板。
- 不得因为仓库已有某个模型脚本就优先推荐它，也不得把未列出的模型视为不支持。
- 非平凡的新实现先检查当前仓库及成熟开源库；综合相关性、维护状态、许可证与集成成本后优先复用。
- 新模型应接入统一实验产物：配置、交叉验证指标、逐样本预测、模型文件、运行清单和 PNG/SVG/PDF 图片。

## 工作流程

1. `01_data`：加载原始附件，检查类型、缺失、重复、异常和泄漏风险。
2. 判断任务结构：普通表格任务进入 `02_tabular`；时间序列、异常检测、聚类分别进入 `04`、`05`、`06`。
3. `03_feature`：只根据训练数据和业务机制构造特征；所有会学习参数的变换必须放进 Pipeline。
4. `07_cv`：根据任务选择随机、分层、分组或时间顺序验证。模板没有 GroupKFold 时应主动增加，而不是误用随机切分。
5. 候选模型至少包含可解释基线和一个与数据机制匹配的模型；需要时添加任意第三方或自定义模型。
6. `08_visualization`：围绕结论和误差诊断作图，不为凑图而画图。
7. `09_report`：汇总可复现实验产物，说明假设、指标、误差来源、敏感性和适用边界。

## 扩展模型

优先通过配置的 `custom_models` 注册实现 sklearn `fit/predict` 接口的估计器。若模型接口不同，可在 `02_tabular` 新增适配脚本或在 `src/mathmodel` 增加适配器。允许扩展预处理、指标、验证器和任务类型，不要求迁就现有抽象。

